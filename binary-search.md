# 二分搜尋法 (Binary Search)

## 1. 演算法核心概念
這個章節是一個重要的演算法 叫做二分搜尋法
這個搜尋方法的時間複雜度為O(log n) 是非常快的搜尋方法
不過他有個必要條件 也就是單調性

單調性是什麼？ 單調性就是在說一串數列按照順序掃過去之後是遞增或遞減的
舉例來說
陣列A [1,2,3,4,5] 具有單調性
假設目前位置為ai 可以發現a1到i-1 也就是ai前面的數字必定小於等於ai 而ai+1到a5 也就是ai後面的數字必定大於等於ai
由此可知
陣列B [1,3,5,6,7,7,9] 也是一個具有單調性的陣列
而陣列C [9,8,7,4,2,-1,-1,-5] 也是一個具有單調性的陣列
但是陣列D [1,2,3,4,5,3] 並不具有單調性
原因就是這個陣列並非遞增或遞減
若在位置5 往左會是4 往右會是3
可知4<=5<=3 以及 4>=5>=3都不成立
補充一下 實作時由於通常會sort 而sort是由小到大排序 所以通常會是由左到右遞增

知道了單調性 接下來我來解釋二分搜的區間縮減
二分搜的原理是這樣
有一個陣列A [1,3,5,7,9] 由於具有單調性 可以使用二分搜
你想要找7的位置在哪 於是你詢問陣列的中點(5)是否小於目標搜尋值(7)
若中點小於目標值 (待續..)

## 2. 邊界條件與閉開區間推導
- 閉區間 `[L, R]` vs. 左閉右開 `[L, R)`
- 指針位移決策：`mid`、`mid + 1`、`mid - 1`
- 防範整數溢位：`mid = L + (R - L) / 2`

## 3. 極限測資與收斂性探討
- 目標值不存在時的指針終止位置
- 陣列包含重複元素時的行為
- 極短陣列（長度 0 或 1）

## 4. 狀態機與可視化展示
*(待組件開發後嵌入)*